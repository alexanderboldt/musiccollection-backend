quarkus:
  devservices:
    enabled: true
  oidc:
    enabled: false
  datasource:
    db-kind: postgresql
    username: test
    password: test
    devservices:
      image-name: postgres:17.4
  flyway:
    enabled: true
    migrate-at-start: true
  s3:
    devservices:
      enabled: false
    path-style-access: true
    aws:
      region: eu-central-1
      credentials:
        type: static

bucket:
  artist: artist
  album: album
