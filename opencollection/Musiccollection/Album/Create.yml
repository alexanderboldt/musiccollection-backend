info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/v1/albums"
  body:
    type: json
    data: |-
      {
        "artistId": "1",
        "name": "Nevermind",
        "year": 1999,
        "tracks": 12
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
