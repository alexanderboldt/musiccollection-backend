info:
  name: Get All
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/v1/albums?sort=id"
  params:
    - name: sort
      value: id
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
