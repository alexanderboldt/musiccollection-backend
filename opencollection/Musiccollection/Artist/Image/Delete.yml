info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/v1/artists/:id/images"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
