apiVersion: v1
kind: Service
metadata:
  name: musiccollection-minio
spec:
  selector:
    app: musiccollection-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: musiccollection-minio
spec:
  selector:
    matchLabels:
      app: musiccollection-minio
  serviceName: musiccollection-minio
  replicas: 1
  template:
    metadata:
      labels:
        app: musiccollection-minio
    spec:
      containers:
        - name: musiccollection-minio
          image: minio/minio:latest
          imagePullPolicy: IfNotPresent
          args:
            - server
            - /data
            - --console-address=:9001
          envFrom:
            - secretRef:
                name: musiccollection-minio-secret
          ports:
            - name: api
              containerPort: 9000
            - name: console
              containerPort: 9001
          volumeMounts:
            - name: musiccollection-minio-storage
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: musiccollection-minio-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 1Gi
