apiVersion: v1
kind: Service
metadata:
  name: musiccollection-postgres-keycloak
spec:
  selector:
    app: musiccollection-postgres-keycloak
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: musiccollection-postgres-keycloak
spec:
  selector:
    matchLabels:
      app: musiccollection-postgres-keycloak
  serviceName: musiccollection-postgres-keycloak
  replicas: 1
  template:
    metadata:
      labels:
        app: musiccollection-postgres-keycloak
    spec:
      containers:
        - name: musiccollection-postgres-keycloak
          image: postgres:17.4
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: musiccollection-postgres-keycloak-secret
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: musiccollection-postgres-keycloak-storage
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: musiccollection-postgres-keycloak-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 1Gi
